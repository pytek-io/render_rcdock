DockLayout:
  ref_hook: "ref"
  create_stub: true
  arguments:
    defaultLayout:
      description: "default layout."
      argument_type: "dict"
    onKeyPress:
      description: "onKeyPress."
      argument_type: "void"
  methods:
    dockMove:
      description: "Move a dock to a new position."
      method: "dockMove"
    saveLayout:
      description: "Save the current layout."
      method: "saveLayout"
    loadLayout:
      description: "Load a layout."
      method: "loadLayout"